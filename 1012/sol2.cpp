#include <iostream>

using namespace std;

int main()
{
  int k ,n, i; // i denotes how many people left
  int x0, x1, m, flag;
  while(cin >> k && k != 0)
  {
    n = 2 * k;
    x0 = 1;
    m = 0;
    flag = 1;
    while (flag)
    {
      m++;
      for (i = 2; i <= k;i++)
      {
        if(x0 > k)
          break;
        
        
      }
      
    }
  
  }

  return 0;
}